import { JoiValidationPipePipe } from './joi-validation-pipe.pipe';

describe('JoiValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new JoiValidationPipePipe()).toBeDefined();
  });
});
